import GifUtils from './gifutils';

// eslint-disable-next-line no-unused-vars, @typescript-eslint/no-unused-vars
((_) => {
  // eslint-disable-next-line no-undef
  ((window as unknown) as { EmoteReplacer: { GifUtils: unknown } })
    .EmoteReplacer.GifUtils = GifUtils;
})();
